import 'package:flutter/material.dart';

class Paddings {
  final listViewPaddings =
      const EdgeInsets.only(right: 10.0, left: 10.0, bottom: 5.0);
  final searchBoxPaddings =
      const EdgeInsets.symmetric(vertical: 20.0, horizontal: 20.0);
}
